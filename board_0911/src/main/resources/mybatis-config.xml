<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/> <!-- underscore_case와 camelCase 매핑 -->
    </settings>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/> <!-- 트랜잭션 매니저 설정 -->
            <dataSource type="POOLED"> <!-- POOLED 데이터 소스 -->
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/homelist?serverTimezone=Asia/Seoul"/><property name="url" value="jdbc:mysql://localhost:3306/homelist?serverTimezone=Asia/Seoul"/>
                <property name="username" value="root"/>
                <property name="password" value="1107"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/board-mapper.xml"/> <!-- 매퍼 XML 파일 경로 -->
    </mappers>

</configuration>